# Legacy pip fallback — primary source is now pyproject.toml + uv.lock.
# Prefer: `uv sync` (see README). This file is kept for `pip install -r requirements.txt`.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
pyyaml==6.0.2
jinja2==3.1.5
Markdown==3.10.2
openpyxl==3.1.5
python-docx==1.1.2
pandas==2.2.3
pytest==8.3.4
pytest-asyncio==0.25.0

# Optional — only needed to run the browser end-to-end suite
# (tests/test_web_e2e.py). Install with:
#   pip install playwright==1.49.1 && playwright install chromium
